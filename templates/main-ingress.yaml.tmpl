apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: main-ingress
  namespace: default
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - '*.{{.Node.Domain}}'
        - '{{.Node.Domain}}'
      secretName: tls-secret
  rules:
    - host: '*.{{.Node.Domain}}'
